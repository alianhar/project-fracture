# Default untuk build produksi (`npm run build`). Di-commit sengaja --
# ini deklarasi eksplisit, bukan kecelakaan. Backend asli sudah live di
# Cloud Run (2026-08-20, lihat CLAUDE.md) -- deploy publik sekarang jalan
# di atas model ConvNeXt sungguhan, bukan mock lagi.
VITE_API_BASE_URL=https://fracture-api-607128796608.asia-southeast2.run.app
VITE_USE_MOCKS=false
